# Lambda-specific requirements (extends base requirements.txt)
# Core FastAPI dependencies (matching main requirements.txt)
fastapi==0.104.1
python-multipart==0.0.6
openai==1.3.5
python-docx==1.1.0
PyMuPDF==1.23.8
python-dotenv==1.0.0
httpx==0.25.2

# Lambda-specific additions
mangum==0.17.0

# Optional: AWS SDK (usually pre-installed in Lambda)
# boto3==1.34.0
# botocore==1.34.0